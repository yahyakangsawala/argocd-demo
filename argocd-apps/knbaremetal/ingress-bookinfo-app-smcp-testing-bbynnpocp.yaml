apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ingress-book-info-app-smcp-testing-knbaremetal
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/compare-options: IgnoreExtraneous
    argocd.argoproj.io/sync-options: Prune=false
spec:
  generators:
  - list:
      elements:
      - cluster: knbaremetal
        url: https://api.knbaremetal.kuwaitnet.lab:6443
        values:
          name: bookinfo-application-knbaremetal
          chartPath: appplications-demo/ingress/bookinfo
          valuesFile: values-bookinfo-app-smcp-testing-knbaremetal.yaml
      - cluster: knbaremetal
        url: https://api.knbaremetal.kuwaitnet.lab:6443
        values:
          name: istio-ingress-resources-knbaremetal
          chartPath: istio-apps-resources
          valuesFile: values-ingress-app-smcp-testing-knbaremetal.yaml
  template:
    metadata:
      name: '{{values.name}}'
      annotations:
        argocd.argoproj.io/compare-options: IgnoreExtraneous
        argocd.argoproj.io/sync-options: Prune=false
      labels:
        gitops: operator
    spec:
      project: default
      # syncPolicy:
      #   automated:
      #     prune: false
      #     selfHeal: true
      #   retry:
      #     limit: 5
      #     backoff:
      #       duration: 5s
      #       factor: 2
      #       maxDuration: 20m
      source:
        repoURL: https://github.com/yahyakangsawala/argocd-demo.git
        targetRevision: master
        path: '{{values.chartPath}}'
        helm:
          valueFiles:
            - '{{ values.valuesFile }}'
      destination:
        server: https://api.knbaremetal.kuwaitnet.lab:6443
        namespace: openshift-gitops