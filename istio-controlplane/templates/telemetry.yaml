apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: default
  namespace: {{ .Values.namespace }}
spec:
  accessLogging:
    - providers:
        - name: envoy
  metrics:
    - overrides:
        - disabled: false
      providers:
        - name: prometheus
  tracing:
    - providers:
        - name: otel
      randomSamplingPercentage: 100